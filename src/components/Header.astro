<header class="fixed top-0 left-0 w-full flex justify-end items-center h-16 bg-black bg-opacity-40 px-36 z-50 shadow">
    <nav class="justify-center items-center gap-8 hidden sm:flex">
        <slot></slot>
    </nav>
</header>